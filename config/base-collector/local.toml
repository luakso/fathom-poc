env = "local"
secrets_path = "config/base-collector/local.secrets.toml"

[log]
is_pretty = true
level = "debug"

# hypersync_bearer_token lives in local.secrets.toml (gitignored)
# or comes from BASE__HYPERSYNC_BEARER_TOKEN.
